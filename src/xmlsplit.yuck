Usage: xmlsplit [FILE]...

Split big XML files into form-feed (\f) separated chunks.

  --by=NODE         Output trees with NODE as the root node.
                    By default the first child of the root is
                    selected as pivot element.
