Usage: gencls [OPTIONS]... UnicodeData.txt

Generate classifier bitfields.

  -w, --width=N         Output only unicode characters of UTF8-width N
  --upper-lower-maps    Output upper->lower mapping code
  --bitfields           Output bit fields instead of switch cases
